# UN260 开机亮屏优化回退记录

用户于 2026-09-14 明确要求：保留开始“加快开机显示”需求之前的代码，出现问题时可退回该状态。该基线不同于“只回退上一轮优化”，也不是原先未包含本地修改的 HEAD。

## 三个明确回退点

| 用途 | 本地 Git 标签 | 提交 |
|---|---|---|
| 完全回到加快亮屏需求之前 | `codex/baseline-before-fast-boot-20260914` | `a2393fd4de392a2090f745c97cebbc727f99cb85` |
| 保留提前亮屏，撤销最新稳定性优化 | `codex/boot-early-before-stability-20260914` | `d8d2890f75f11a919866484b4c5c07cfc53a9ee5` |
| 本次稳定性优化完整代码 | `codex/boot-stable-20260914` | `a91bb1c57616815d3ea0a1dcc92c92b70a6563dc` |

不要在用户仅说“回退”时混淆这三个范围。实际操作前重新核对用户希望保留的后续功能、当前未提交数据以及板上镜像版本。

## 原始基线的证据

- 历史基础提交：`4eb3196052af7202f217d07a51d30668aa16b9f3`。
- Windows 历史交付：`deliveries/20260914_startup_fast/backup/before-dirty.tar.gz` 与 `before.patch`。
- 该备份保存了亮屏加速开始之前的 26 个本地修改/新增文件，包括主界面详情点击区、币种面额和自检修复、第三套同步渐显动画。
- 按三个功能提交恢复后，逐文件比对备份内容，并核对 before.patch 的目标 Git blob；全部一致。
- 在独立导出目录交叉编译该标签下的应用成功。使用标签中的应用和 aic-mpp 源码，以及已安装的 SDK 第三方头文件/库；没有借用新开机代码。该验证不是旧完整系统镜像已重新构建或上板验证。

## 本次本地提交拆分

1. `d38673b5b`：主界面详情入口与大写标签。
2. `537474330`：自检回复与币种面额数据流。
3. `a2393fd4d`：第三套渐显开机动画；在此固定加速前基线。
4. `9cecf0424`：DBus ELF 净化与启动错误。
5. `df0cff1a0`：历史索引异步读取与计数背压。
6. `aa7299b13`：面板重复 DSI 使能修正及回退参数。
7. `d8d2890f7`：系统异步准备、原生首屏与 initramfs 提前显示。
8. `e2b37987f`：第三套动画刷新节拍。
9. `d447e40a3`：原生 RAM 合成、小范围重绘及统计基础。
10. `a91bb1c57`：可选启动帧统计、生命周期、增量构建依赖和生成资源忽略规则。

共享文件使用对应历史快照分阶段进入索引，未通过覆盖当前工作文件制造提交。既有业务修改保留；未包含 Windows SSH 密钥、设计稿、交付镜像或生成的 boot-light.bin。补丁格式中必需的空白上下文行采用单文件 Git 属性保留，未篡改已验证的 patchelf 补丁。

## 安全回退原则

1. 不立即对主工作区使用 reset --hard。先保留届时新增提交、未提交修改及设备配置/历史数据。
2. 从明确标签在独立工作目录准备源码，核对依赖。回到加速前标签会同时退回后来引入的历史异步加载、DBus 修复、DSI 修正等，不应误称只撤销动画。
3. 系统级回退要使用重新配置的独立构建输出，重新生成匹配内核、根文件系统和应用。新版本生成的内核 `.config`、initramfs 列表及构建缓存不受 Git 标签管理；不能只切源码后沿用它们。
4. 只升级 UI 包不能移除新内核中的 initramfs。按板子实际 NAND 几何参数选择完整镜像，遵守原有升级与数据备份流程。
5. 刷机前核对镜像内核、应用、库和资源一致性；刷机后回归冷启动、触摸、点钞、协议、存储及异常自检。不能凭 Git 回退或编译成功承诺板上无影响。

标签、提交及本文均保存在 Linux 权威仓库 `/home/pc/d213-100ask/d211`；Windows 交付记录及增量 Git bundle 另存于 `deliveries/20260914_functional_commits`。本轮仅本地提交，不推送、不刷机、不实际回退。
