# Configuration 提前激活与 Ready 缩短

## 修改

- Configuration 不再显示 Waiting。进入自检后即使用 Checking 文案；等待页面所有卡片入场完毕（970 ms）后，开始原有 720 ms 浮起/高亮动画。
- 只提前视觉状态。不发送握手/自检命令、不生成结果、不增加 completed_count，真实结果未到时百分比保持 0%。
- Configuration 活跃期间持续小点呼吸，避免原三轮后静止。其他项目保持原逻辑。取消重复的全局握手转圈，保留 Connecting to controller 文案。
- 收到 Configuration 成功/失败结果立即按真实结果显示；服务进入 FAIL（包括握手超时）时将该活动项显示失败，不会一直伪装 Checking。其他卡片不得被提前成功，全部真实通过才能 Ready。
- Ready 标题完全显示后目标停留 600 ms（成功后 2190+600=2790 ms）。此前停留随预热变化，无法数学上固定减半；本次改为短停留目标。
- 目标 Main/Pure 已预热则不再等待其他隐藏页全部预热。目标页尚未就绪、会话重置延迟或首帧耗时可延长停留，不能为了时长跳过业务准备。
- 保留上一版首帧完成后才开始渐显计时的修复；不重新引入 Main 分区位移。提前亮屏和 Welcome 时间轴不变。

## 验证及边界

真实 LVGL 主机渲染：等待0%状态、Configuration 浮起延迟、长时间活动、结果失败/服务失败、成功就绪、退出清理、计时门槛通过。协议/运行时：只预热目标页即允许结束等待、Ready 保持门槛、失败/超时/截止、面额保留回归通过。四套主题入口检查及完整 make 通过。

162 份载荷校验并与构建 target 比对通过。尚未在实机验证帧率、通信时序和视觉；未刷机、未提交 Git。通过 UPK 更新，无需 IMG。

权威路径：`/home/pc/d213-100ask/d211/output/d211_d213_devkitf/images/UN260_UPDATE.upk`

SHA256：`428da6c6e66c0009909d2f1104b19fbc0a835c78dc9d694ce26401de6f4f1ef7`
