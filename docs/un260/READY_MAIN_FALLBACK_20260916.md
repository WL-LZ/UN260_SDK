# Main 入场回退 · 2026-09-16

用户实机反馈上一包分区入场卡顿。本包优先恢复已使用过的 Main 入场，不继续叠加未经板测的动效优化。

- 完整撤销本轮 Main 分区位移、错时、回落、专用定时器与接口。Main 的 .c/.h 已与当前 HEAD（本次入场修改之前）逐字节核对一致。
- 保留新的英文就绪页：Ready to count. / Precision starts here.。
- 就绪提示先用 360 ms 退场，400 ms 起使用原来的 600 ms 单张背景淡出；约 1000 ms 清理交接遮罩，恢复上一版 Main 出场方式。
- 没有修改主控协议、面额逻辑、币种页、提前亮屏、Welcome 动画、主界面布局和日常交互。未修改 HTML 参考稿。
- 不声称已经测得帧率改善：目前没有新板测数据。已移除新增的大范围几何移动开销，恢复旧路径，仍需实机确认。

升级使用同目录 UN260_UPDATE.upk，替换 U 盘 update 目录内旧包。此次应用层变更无需重新烧录 IMG。上一目录 20260916_ready_main_motion 是历史版本，不要继续使用那个包。

未刷机、未提交 Git；旧的未跟踪 boot-light.bin 保留。

## 验证

四套开机宏检查、真实 LVGL 渲染、静止零新增重绘、失败保持、Pure/异常切页、定时器失败清理通过。新增断言核对 700 ms 时淡入约一半、1010 ms 时交接资源全部释放。协议恢复与面额保留回归通过。

应用重新交叉编译、完整 make 成功。162 份载荷内容/权限与构建 target 一致（版本文件单独验证）。

SHA256：`f35b63b849d5de6a4b5c76a5d6b6660be12b8e0838a6f22adcf2e028bee32251`
