# 持续自检转圈与 Ready 缩短

- Getting ready 下方的蓝色转圈在整个自检阶段持续显示，包括握手成功后的逐项检查。真实全部成功进入 Ready，或失败/超时、隐藏、销毁时停止并删除，避免假装仍在正常检查。
- Configuration 提前 Checking 与小点呼吸保留，进度仍以真实结果为准。
- Ready to count. 渐显 800 → 533 ms，渐隐 360 → 240 ms（各缩短三分之一）。其他元素的渐显不改；退场随 Ready 一起缩短。
- 完整显示后的目标停留仍为 600 ms，门槛随新的渐显结束点同步为自检成功后 2523 ms。实际准备不足时仍可延长。
- Main 的渐显仍为 600 ms：Ready 退场完成后留 40 ms 间隔，再开始；过渡总时长从 1000 → 880 ms。保留首帧完成后再计时，不恢复分区位移。

仅修改应用层，可用本目录 UN260_UPDATE.upk 更新，无需烧录 IMG。Linux 权威输出同样更新：
`/home/pc/d213-100ask/d211/output/d211_d213_devkitf/images/UN260_UPDATE.upk`

未刷机、未提交 Git；实际板上帧率仍需验证。

验证：完整 make、四套主题入口、真实 LVGL 持续转圈/失败停止/Ready 静止零新增重绘、过渡清理与预热回归通过；162 个升级载荷与构建 target 内容及权限匹配。

SHA256：`c2a099ae241c80735a04cbdf36593ec4086274e761567aef9d7e874b0440543c`
